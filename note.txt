

Backend
Create virtual environment

python -m venv venv

Activate

Windows

venv\\Scripts\\activate

Linux

source venv/bin/activate

run
uvicorn app.main:app --reload

http://localhost:8000

{
  "message":"AI Audio Enhancer API"
}

Frontend

PS C:\Users\shrijandra\youtube-ai-enhancer\frontend> node -v
v24.18.0
PS C:\Users\shrijandra\youtube-ai-enhancer\frontend> npm -v
11.16.0

npm run dev
http://localhost:5173

to increase timeout
src/services/api.ts

for 2minutes
const api = axios.create({
  baseURL: "http://localhost:8000",
  timeout: 120000,
});

while doing git push
make sure to clean up upload and output folder and temp folder that contains video file.

src\components\enhancement\PresetSelector.tsx
className="w-full rounded-md border border-slate-700 bg-slate-900 text-white p-3 focus:outline-none focus:ring-2 focus:ring-blue-500"
label className="block mb-2 font-medium"

 {afterAnalysis && (
  <>
<CreatorScoreCard score={calculateCreatorScore(afterAnalysis)} />
    <AudioAnalysisDashboard analysis={afterAnalysis} />
    <AIRecommendations analysis={afterAnalysis} /> 

deleted
from render in airecommendation.tsx
<div className="mb-5 rounded-xl border border-indigo-700/60 bg-slate-950 p-5">
        <p className="text-sm text-slate-400">Creator Audio Score</p>
        <p className="mt-2 text-4xl font-bold text-white">
          {finalScore}
          <span className="text-xl text-slate-400">/100</span>
        </p>
      </div>
